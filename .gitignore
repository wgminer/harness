# Dependencies
node_modules

# Build outputs
dist/
dist-web/
dist-ssr/
out/
src-tauri/target/
build/icon.iconset/
*.tsbuildinfo

# Environment & signing
.env
.env.*
!.env.example
*.local
*.p12

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor & OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local scratch / imports (not part of the repo)
import/
import_v2/
backlog/

# Legacy v1 web app (ignore if a stray file remains locally)
firebase.js

# Test coverage (Vitest)
coverage/
.e2e-user-data/
playwright-report/
test-results/

# Git worktrees (local agent branches)
.worktrees/

# Agent workflow (local only)
.cursor/
docs/plans/
docs/superpowers/

# macOS native helpers (built by prebuild)
resources/HarnessSpeech
resources/HarnessFnMonitor

# Removed Parakeet stack (local build artifacts only — safe to delete)
resources/parakeet/
build/parakeet-cpp/
build/parakeet-cache/
build/parakeet-venv/

# Swift Package build artifacts (native helpers)
native/**/.build/
native/**/.swiftpm/

# iOS / Xcode
**/xcuserdata/
*.xcuserstate
ios/**/DerivedData/
ios/.build/
ios/.build-device/
ios/**/.swiftpm/
