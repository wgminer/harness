/** Width at or below which main treats the window as the \"small\" preset (`window:toggleSize` expands). */
export const WINDOW_SMALL_PRESET_MAX_WIDTH_PX = 400;

/** Viewport width at or below which the sidebar is off-canvas and compact layout applies. */
export const LAYOUT_COMPACT_MAX_WIDTH_PX = 768;
