Releases no longer commit DMGs here.

The download page links users to the latest GitHub release. On that page, download the **`.dmg`** (`harness-vX.Y.Z-mac.dmg`). The zip, `.app.tar.gz`, signature, and `latest.json` are not the installer.
